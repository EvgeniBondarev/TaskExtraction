# Опционально: cp docker-compose.override.example.yml docker-compose.override.yml
# Compose подхватывает override автоматически. На VPS прод используйте redeploy-prod.sh, не compose.

services:
  api:
    environment:
      PUBLIC_API_URL: http://localhost:8000
      CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
